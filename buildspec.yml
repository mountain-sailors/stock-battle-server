version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm install
      - pwd
    run-as: ec2-user

artifacts:
  files:
    - "**/*"
cache:
  paths:
    - "node_modules/**/*"
